// the sensor
// checks geometry, like is the object in the FOV? in the range we set it
// probabillistic detection using rcs and range to calc this. the radar equation
// add more noise to this position
